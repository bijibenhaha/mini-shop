store {
  mode = "db"
  db {
    datasource = "druid"
    dbType = "mysql"
    driverClassName = "com.mysql.cj.jdbc.Driver"
    url = "jdbc:mysql://mysql:3306/seata?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai"
    user = "root"
    password = "root"
    minConn = 5
    maxConn = 30
    globalTable = "global_table"
    branchTable = "branch_table"
    distributedLockTable = "distributed_lock"
    lockTable = "lock_table"
    queryLimit = 100
    maxWait = 5000
  }
}

service {
  vgroupMapping {
    default = "default"
  }
  default {
    grouplist = "127.0.0.1:8091"
  }
  enableDegrade = false
  disableGlobalTransaction = false
}

client {
  rm {
    asyncCommitBufferLimit = 10000
    lock {
      retryInterval = 10
      retryTimes = 30
      retryPolicyBranchRollbackOnConflict = true
    }
    reportRetryCount = 5
    tableMetaCheckEnable = false
    reportSuccessEnable = false
  }
  tm {
    commitRetryCount = 5
    rollbackRetryCount = 5
    defaultGlobalTransactionTimeout = 60000
    degradeCheck = false
  }
  undo {
    dataValidation = true
    logSerialization = "jackson"
    onlyCareUpdateColumns = true
  }
  log {
    exceptionRate = 100
  }
}

transport {
  serialization = "seata"
  compressor = "none"
}

server {
  maxCommitRetryTimeout = -1
  maxRollbackRetryTimeout = -1
  rollbackRetryTimeoutUnlockEnable = false
  distributedLockExpireTime = 10000
  xaerNotaRetryTimeout = 60000
}

metrics {
  enabled = false
  registryType = "compact"
  exporterList = "prometheus"
  exporterPrometheusPort = 9898
}